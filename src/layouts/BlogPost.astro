---
import BaseLayout from "./BaseLayout.astro";
import FormattedDate from "../components/FormattedDate.astro";

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags?: string[];
}
const { title, description, pubDate, updatedDate, tags = [] } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <article class="prose">
    <header class="post-header">
      <p class="kicker">// DEVLOG</p>
      <p class="post-meta">
        <FormattedDate date={pubDate} />
        {
          updatedDate && (
            <span class="muted"> · updated <FormattedDate date={updatedDate} /></span>
          )
        }
      </p>
      <h1>{title}</h1>
      {description && <p class="post-lede muted">{description}</p>}
      {
        tags.length > 0 && (
          <ul class="hud-tags post-tags">
            {tags.map((t) => (
              <li>{t}</li>
            ))}
          </ul>
        )
      }
    </header>
    <slot />
  </article>

  <p class="back"><a href={import.meta.env.BASE_URL.replace(/\/$/, "") + "/blog"}>← All posts</a></p>
</BaseLayout>

<style>
  .post-header {
    margin-bottom: 2.5rem;
  }
  .post-meta {
    color: var(--text-faint);
    font-family: var(--font-mono);
    font-size: 0.85rem;
    margin-bottom: 0.75rem;
  }
  .post-lede {
    font-size: 1.15rem;
    margin-top: 0.5rem;
  }
  .post-tags {
    margin-top: 1rem;
  }
  .back {
    max-width: var(--measure);
    margin: 3rem auto 0;
  }
</style>
