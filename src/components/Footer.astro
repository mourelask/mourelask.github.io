---
import { SITE_TITLE, SOCIAL_LINKS } from "../consts";
const year = new Date().getFullYear();
const base = import.meta.env.BASE_URL;
const feedHref = (base.replace(/\/$/, "") + "/rss.xml").replace(/^$/, "/");
---

<footer class="site-footer">
  <div class="container footer-inner">
    <p class="muted">© {year} {SITE_TITLE}</p>
    <nav aria-label="Social">
      <ul>
        {
          SOCIAL_LINKS.map((link) => (
            <li>
              <a href={link.href} rel={(link as any).rel ?? undefined}>
                {link.label}
              </a>
            </li>
          ))
        }
        <li><a href={feedHref}>RSS</a></li>
      </ul>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--border);
    padding-block: 2rem;
    margin-top: 4rem;
  }
  .footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
  }
  .footer-inner p {
    margin: 0;
    font-size: 0.9rem;
  }
  ul {
    list-style: none;
    display: flex;
    gap: 1.2rem;
    margin: 0;
    padding: 0;
  }
  a {
    color: var(--text-muted);
    font-size: 0.9rem;
  }
  a:hover {
    color: var(--accent);
    text-decoration: none;
  }
</style>
